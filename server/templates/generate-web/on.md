The existing puzzles below are your main seed. Read them first: they show the style, the length and the
mechanisms already covered, so aim for settings and mechanisms the list does not have yet.

Web search is available, but it is a last resort rather than a first step. With {{SEED_MIN}} or more
existing puzzles you have enough to work from and should not need it at all. Search only when the list is
too small to give you a feel for the game, or to check a real-world fact a puzzle relies on. You may look up
raw material (a science or history anecdote, an odd fact about a trade or a place) and build something new
from it; never search for lateral thinking puzzles to adapt, since anything you find that way is a known
puzzle. Keep to a few queries.
